{
  "rules": {
    "users": {
      ".read": "auth !== null",
      "$uid": {
        ".write": "$uid === auth.uid"
      }
    },
    "chatrooms": {
      ".read": "auth !== null",
      "$chatroomId": {
        ".write": "auth !== null && (!data.exists() || data.child('createdBy').val() === auth.uid)",
        "members": {
          "$uid": {
            ".write": "auth !== null && (data.parent().parent().child('members').child(auth.uid).exists() || $uid === auth.uid)"
          }
        }
      }
    },
    "messages": {
      "$chatroomId": {
        ".read": "auth !== null && root.child('chatrooms').child($chatroomId).child('members').hasChild(auth.uid)",
        ".write": "auth !== null && root.child('chatrooms').child($chatroomId).child('members').hasChild(auth.uid)"
      }
    }
  }
}